# Copy this file to .env and replace every CHANGE_ME value.
#   cp .env.example .env
# Generate strong secrets with:  openssl rand -hex 32

# --- Open Notebook ---
# Encrypts API credentials stored in the database. Must be long & random.
OPEN_NOTEBOOK_ENCRYPTION_KEY=CHANGE_ME_run_openssl_rand_hex_32

# Basic-auth login for the Open Notebook UI (used if exposed beyond localhost)
BASIC_AUTH_USERNAME=kevin
BASIC_AUTH_PASSWORD=CHANGE_ME_strong_password

# --- SurrealDB ---
SURREAL_USER=root
SURREAL_PASSWORD=CHANGE_ME_strong_db_password

# --- Cloud models via OpenRouter (optional) ---
# For DeepSeek-V3.2 (notebook) and Kimi K2.6 (coding). Leave blank to stay
# fully local. Get a key at https://openrouter.ai/keys . See MODELS.md.
OPENROUTER_API_KEY=

# --- Composio action layer (optional) ---
# Lets the assistant take authenticated SaaS actions (Gmail, Calendar, ...).
# NOTE: managed auth is cloud-based — see integrations/composio/README.md.
COMPOSIO_API_KEY=
